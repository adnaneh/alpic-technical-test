# API Environment Variables
NODE_ENV=production
PORT=3000

# OpenAI Configuration
# Set your OpenAI API key here (required)
OPENAI_API_KEY=

# Optional model & step limits (defaults: model=gpt-4o, steps=10)
OPENAI_MODEL=gpt-4o
CHAT_MAX_STEPS=10

# Control provider-side storage of responses (true/false, default true)
OPENAI_STORE=true

# CORS allowlist for API (comma-separated origins)
# Example: CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
# Leave empty to allow all (development default)
CORS_ORIGIN=

# Optional: Database / Auth if added later
# DATABASE_URL=
# JWT_SECRET=


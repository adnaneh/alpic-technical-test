# API Environment Variables
NODE_ENV=production
PORT=3000

# OpenAI Configuration (for API)
OPENAI_API_KEY=your_openai_api_key_here
# Optional model & step limits
# Defaults: model=gpt-4o, steps=10
OPENAI_MODEL=gpt-4o
CHAT_MAX_STEPS=10
# Control provider-side storage of responses (true/false)
OPENAI_STORE=true

# CORS allowlist for API (comma-separated origins)
# Example: CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
# Leave unset to allow all (development default)
CORS_ORIGIN=

# Frontend Environment Variables
NEXT_PUBLIC_API_URL=https://your-api.railway.app

# Optional: Database (if you add one later)
# DATABASE_URL=postgresql://user:password@host:port/database

# Optional: JWT Secret (if you add auth)
# JWT_SECRET=your_jwt_secret_here
